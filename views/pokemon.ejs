<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title><%= pokemon.name %> - Pokédex</title>
    <style>
        .bg-primary {
            background-color: #ff0019 !important;
        }
        .card {
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgb(255, 255, 255);
        }
        .pokemon-img {
            width: 300px;
            border-radius: 100%;
            border: 3px solid #ff1e00e5;
        }
        .btn-custom {
            background-color: #ff0000;
            color: white;
            border-radius: 20px;
        }
        
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="card mx-auto text-center" style="max-width: 400px;">
            <div class="card-header bg-primary text-white">
                <h1 class="h4 text-capitalize mb-0"><%= pokemon.name %></h1>
            </div>
            <div class="card-body">
                <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>" class="pokemon-img mb-3">
                <p class="mb-2"><strong>Altura:</strong> <%= pokemon.height %> cm</p>
                <p><strong>Peso:</strong> <%= pokemon.weight %> kg</p>
                <a href="/" class="btn btn-custom mt-3 px-4">Voltar para a lista</a>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
